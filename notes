# https://trac.ffmpeg.org/wiki/Capture/ALSA
# https://trac.ffmpeg.org/wiki/Capture/Desktop
$ ffmpeg -video_size 1920x1080 -framerate 30 -f x11grab -i :0.0 -f alsa -ac 1 -i loopout out.mkv

$ cryptsetup luksOpen /dev/sdX mapName
$ mount /dev/mapper/mapName /mountpoint
$ chown user:user /mountpoint
$ umount /home/user/data mapName
$ cryptsetup close mapName

make
$ sudo make install
$ sudo make uninstall
$ make clean
or
$ sudo checkinstall



DISPLAY
$ xrandr -q
$ xrandr --output HDMI1 --mode 1920x1080
$ xrandr --output eDP1 --right-of  HDMI1

UTILS
diskpart

$ wget http://ipinfo.io/ip -qO -  
$ curl ipecho.net/plain
$ scp your_username@remotehost.edu:foobar.txt /local/dir
$ ncmpcpp (u updates)
$ gcolor
$ pavucontrol
$ lspci -k
$ whereis
$ find / -name foo
$ xbacklight -set 90
$ ghex
$ cat file | cut -d ":" -f 1 > out
$ 7z a -pPASS output.7z files.txt
$ java -jar jpodder1.0RC2-Linux.jar
$ mplayer -vo png -frames 1 tv://
$ sudo mount -o gid=wheel,fmask=113,dmask=002 /dev/sdb1 /mnt/usb16
$ sudo dd if=filename.iso of=/dev/usbdevice bs=4M; sync
$ feh --bg-scale /path/to/image.file

$ git init
$ git remote add origin https://github.com/monkey/foo.git
$ git add .
$ git pull https://
$ git commit -am "comment"
$ git push -u origin master

$ pacman -Syyu
$ pacman -Qdt packages installed as deps for others but not needed anymore
$ pacman -Qet packages that or not dependancies for other packages
$ pacman -Rns remove package, deps and configs
$ paccache
$ pacman -Ql <package> show details of package

NETWORKING
$ ifconfig - local IP
$ wget http://ipinfo.io/ip -qO -  # public IP
$ iw dev
$ ifconfig wlp1s0 up/down
$ ip link


DECOMPILERS
$ gdb
$ objdump

PHUN
$ proxychains
$ macchanger

$ airmon-ng start wlp1s0
$ airodump-ng mon0 --channel
$ aireplay-ng mon0
$ aircrack-ng

$ airgraph-ng
$ nmap -sP 192.168.1.0/24

WORDLIST GEN
$ perl ~/bin/wg.pl -l 8 -u 64 -v 
abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWX YZ0123456789\`\~\!\@\#\$\%\^\&\*\(\)\-\_\+\=\[\]\;\’\,\.\/\<\>\?\:\”\{\}\|\ > words.txt
$ perl /home/user/bin/wg.pl -l 10 -u 10 -v abcdefghijklmnopqrstuvwxyz1234567890 | sudo aircrack-ng /fun/airodump/home/cap-01.cap -e BTHub3-WQC6 -w -BSSID = 00:FE:F4:5C:BB:60
 
$ netdiscover

$ sqlmap -u {site} --dbs
$ sqlmap -u {site} -D {db} --tables 
$ sqlmap -u {site} -D {db} -T {table} --columns 
$ sqlmap -u {site} -D {db} -T {table} -C {column} --dump


int main(int argc, char** argv)
// argc is the argument count, argv is an array of strings given as arguments, and argv[0] is the name of the program
// for example program --version will result in argc=2 and argv = {"program","--version"}

has anyone really been far even as decided to use even go want to do look more like?
( ͡° ͜ʖ ͡°)

0273102420
